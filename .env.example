# Postgres runs on 5433 to avoid clashing with a local instance (see docker-compose.yml)
DATABASE_URL="postgresql://scms:scms@localhost:5433/scms?schema=public"

# Any long random string. Used to sign the JWT session cookie.
AUTH_SECRET="dev-secret-change-me-dev-secret-change-me"

# Optional. With no key the system runs fully on the deterministic engine (Layer 2 rules).
GROQ_API_KEY="gsk_bz7hE2kU9P0y2H1QGapHWGdyb3FYCjyaKruBMg8xHIOIU0itampered"
GROQ_MODEL="openai/gpt-oss-20b"
